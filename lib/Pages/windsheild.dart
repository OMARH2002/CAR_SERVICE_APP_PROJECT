import 'package:flutter/material.dart';

class Windsheild extends StatelessWidget {
  const Windsheild({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Windsheild'),
        backgroundColor: Colors.red,
      ),
    );
  }
}
