import 'package:flutter/material.dart';

class RecordsNav extends StatelessWidget {
  const RecordsNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Records Data',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
