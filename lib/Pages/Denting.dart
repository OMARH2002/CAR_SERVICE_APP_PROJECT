import 'package:flutter/material.dart';

class Denting extends StatelessWidget {
  const Denting({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Denting'),
        backgroundColor: Colors.red,
      ),
    );
  }
}
