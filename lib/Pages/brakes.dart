import 'package:flutter/material.dart';

class Brakes extends StatelessWidget {
  const Brakes({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('brakes'),
      backgroundColor: Colors.red,
    )
      );
}
}
